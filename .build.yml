image: archlinux
packages:
    - nodejs
    - yarn
sources:
    - https://git.sr.ht/~siarune/siarune.dev
tasks:
    - setup: |
        mv siarune.dev siarune
        yarn install
        cd siarune
        yarn pullConfig
    - build: |
        cd siarune
        yarn buildProd
    - deploy: |
        cd siarune
        yarn deploy
